// using Applications.<%= name %>.Namespace
// using Applications.<%= name %>.Resources

Applications.<%= name %> = Ext.extend(WebDesktop.core.WindowApplication, {
    name: '<%= name %>',
    globalResources: Applications.Resources.Global
});