// using Applications.<%= name %>.<%= name %>

Applications.<%= name %>.MainWindow = Ext.extend(WebDesktop.controls.Window, {
    initComponent: function() {
        /*var config = {
            width: 100,
            height: 100,
        };
        Ext.apply(this, Ext.apply(this.initialConfig, config));*/

		Applications.<%= name %>.MainWindow.superclass.initComponent.apply(this, arguments);
    }
});